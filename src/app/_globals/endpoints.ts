// import { environment } from 'src/environments/environment';
// import { API_MDM_CONTEXT } from '../shared/constants';

const ENDPOINTS: any = {
  navigation: {
    list: 'https://iam.dev.us.ic.blackline.com' + '/api/mdm/' + 'global-nav',
  },

};
export { ENDPOINTS };
